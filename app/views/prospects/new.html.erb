<% content_for(:html_title) { 'Accueil' } %>

<style>
  html {
    background-color: #ffd383;
  }
  .product-box {
    border: 3px solid white;;
  }

  /* general styling */
  :root {
    --smaller: .75;
  }

  html, body {
    height: 100%;
    margin: 0;
  }
</style>

<script type="text/javascript">
  (function () {
  const second = 1000,
        minute = second * 60,
        hour = minute * 60,
        day = hour * 24;

  //I'm adding this section so I don't have to keep updating this pen every year :-)
  //remove this if you don't need it
  let today = new Date(),
      dd = String(today.getDate()).padStart(2, "0"),
      mm = String(today.getMonth() + 1).padStart(2, "0"),
      yyyy = today.getFullYear(),
      nextYear = yyyy + 1,
      dayMonth = "06/20/",
      birthday = dayMonth + yyyy;

  today = mm + "/" + dd + "/" + yyyy;
  if (today > birthday) {
    birthday = dayMonth + nextYear;
  }
  //end

  const countDown = new Date(birthday).getTime(),
      x = setInterval(function() {

        const now = new Date().getTime(),
              distance = countDown - now;

        document.getElementById("days").innerText = Math.floor(distance / (day)),
          document.getElementById("hours").innerText = Math.floor((distance % (day)) / (hour)),
          document.getElementById("minutes").innerText = Math.floor((distance % (hour)) / (minute)),
          document.getElementById("seconds").innerText = Math.floor((distance % (minute)) / second);

        //do something later when date is reached
        if (distance < 0) {
          document.getElementById("headline").innerText = "It's my birthday!";
          document.getElementById("countdown").style.display = "none";
          document.getElementById("content").style.display = "block";
          clearInterval(x);
        }
        //seconds
      }, 0)
  }());
</script>

<div class="container">
  <main id="page1">
    <div>
      <%= image_tag "harry.png", class: "harry_jmg", style: "border: white 3px solid; border-radius: 50%;" %>
      <div class="title">
       Devenez 1% meilleurs chaque semaine grâce aux meilleures connaissances de la 🌍
      </div>
      <div class="subtitle">
        Reçois chaque semaine un condensé des meilleurs livres sur ces 5 thèmes : <br class="hide-mobile">la spiritualité, la psychologie, les finances, la productivité et la philosophie.
      </div>
    </div>
    <div class="container-countdown">
      <div class="container">
        <h1 id="headline">Décompte avant pré-lancement</h1>
        <div id="countdown">
          <ul>
            <li><span id="days"></span>jours</li>
            <li><span id="hours"></span>heures</li>
            <li><span id="minutes"></span>Minutes</li>
            <li><span id="seconds"></span>secondes</li>
          </ul>
        </div>
        <div id="content" class="emoji">
          <span>🥳</span>
          <span>🎉</span>
          <span>🎂</span>
        </div>
      </div>
    </div>
    <div>
      <span class="newsletter product-box">
        <%= form_with model: @prospect do |form| %>
          <div class="_form-content">
            <div class="_form-element _x18687261 _full_width">
              <%= form.text_field :name, placeholder: "Prénom" %>
              <%= form.email_field :email, placeholder: "Adresse email" %>
            </div>
            <div class="_button-wrapper _full_width">
              <%= form.submit "JE ME PRÉ-INSCRIS MAINTENANT *", class: "button-block", style: "margin-bottom:16px; width: -webkit-fill-available;" %>
            </div>
          </div>
        <% end %>


      </span>
      <p class="small-notice">(*) : Se pré-inscrire avant le 22 Juin vous donnera accès à une grosse réduction, une avant-première et un bonus spécial.</p>
    </div>

  </main>
</div>
