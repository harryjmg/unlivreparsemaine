<style>
  /* general styling */
  :root {
    --smaller: .75;
  }

  html, body {
    height: 100%;
    margin: 0;
  }

  .g-recaptcha {
    display:none;
  }

  .error_msg {
    font-size: smaller;
    margin-top: -1rem;
    text-align: left;
    margin-left: 16px;
    color: #a01a1a;
  }
</style>

<div class="container">
  <main id="page1">
    <div>
      <%= image_tag "harry.png", class: "harry_jmg" %>
      <div class="title">
       Devenez 1% meilleurs chaque semaine grâce aux meilleures connaissances de la 🌍
      </div>
      <div class="subtitle">
        Reçois chaque semaine un condensé des meilleurs livres sur ces 5 thèmes : <br class="hide-mobile">la spiritualité, la psychologie, les finances, la productivité et la philosophie.
      </div>
    </div>
    <div>
      <span class="newsletter product-box">
        <%= form_with model: @prospect do |form| %>
          <div class="_form-content">
            <div class="_form-element _x18687261 _full_width">
              <%= form.text_field :name, placeholder: "Prénom" %>
              <% if @prospect.errors[:name].present? %>
                <p class="error_msg"><%= @prospect.errors[:name][0]%></p>
              <% end %>
              <%= form.email_field :email, placeholder: "Adresse email" %>
              <% if @prospect.errors[:email].present? %>
                <p class="error_msg"><%= @prospect.errors[:email][0]%></p>
              <% end %>
            </div>
            <div class="_button-wrapper _full_width">
              <%= form.submit "JE VEUX PARTICIPER ! *", id: "submit-btn", class: "button-block", style: "margin-bottom:16px; width: -webkit-fill-available;" %>
            </div>
          </div>
        <% end %>
      </span>
      <p class="small-notice">
        (*) : En t'inscrivant ici, tu recevras immédiatement par email toutes les informations pour rejoindre le projet.
      </p>
    </div>

  </main>
</div>
